import { View } from "../../components/Themed";
import { RootStackScreenProps } from "../../utils/types/common";

export default function ChatMediaList({
  navigation,
}: RootStackScreenProps<"ChatMediaList">) {
  return <View></View>;
}
